---
#
# in-band peering from the local node to the local node
#
    - name: In-band control peering
      shell: | 
        export KUBECONFIG=/etc/rancher/k3s/merged_kubeconfig.yaml
        liqoctl peer --remote-kubeconfig /tmp/k3s.yaml
